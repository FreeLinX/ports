--- a/usr.bin/compress/compress.c	2026-09-02 00:06:37.597798003 +0400
+++ b/usr.bin/compress/compress.c	2026-09-02 00:06:37.785800823 +0400
@@ -390,8 +390,10 @@
 
 	fs->st_mode &= S_ISUID|S_ISGID|S_IRWXU|S_IRWXG|S_IRWXO;
 
-	TIMESPEC_TO_TIMEVAL(&tv[0], &fs->st_atimespec);
-	TIMESPEC_TO_TIMEVAL(&tv[1], &fs->st_mtimespec);
+	tv[0].tv_sec = fs->st_atim.tv_sec;
+	tv[0].tv_usec = fs->st_atim.tv_nsec / 1000;
+	tv[1].tv_sec = fs->st_mtim.tv_sec;
+	tv[1].tv_usec = fs->st_mtim.tv_nsec / 1000;
 	if (utimes(name, tv))
 		cwarn("utimes: %s", name);
 
@@ -410,12 +412,9 @@
 		cwarn("chown: %s", name);
 
 	/*
-	 * Restore the file's flags.  However, do this only if the original
-	 * file had any flags set; this avoids a warning on file-systems that
-	 * do not support flags.
+	 * FreeLinX/Linux: file flags (chflags) have no equivalent on Linux,
+	 * so the BSD st_flags restore block is omitted entirely.
 	 */
-	if (fs->st_flags != 0 && chflags(name, fs->st_flags))
-		cwarn("chflags: %s", name);
 }
 
 static int
