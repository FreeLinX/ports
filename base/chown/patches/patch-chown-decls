--- a/sbin/chown/chown.c
+++ b/sbin/chown/chown.c
@@ -60,6 +60,12 @@
 #include <unistd.h>
 #include <getopt.h>
 
+/* FreeLinX: NetBSD declares uid_from_user(3) in <pwd.h> and gid_from_group(3)
+ * in <grp.h>; musl does not.  Implemented by libc/gen/pwcache.c (extracted
+ * from the same src set). */
+int	 uid_from_user(const char *, uid_t *);
+int	 gid_from_group(const char *, gid_t *);
+
 static void	a_gid(const char *);
 static void	a_uid(const char *);
 static id_t	id(const char *, const char *);
