--- a/usr.bin/base64/base64.c
+++ b/usr.bin/base64/base64.c
@@ -237,7 +237,9 @@
 
 	if (e == 0)
 		return;
-	errc(EXIT_FAILURE, e, "%scoding failed", decode ? "De": "En");
+	/* FreeLinX: musl lacks errc(3); use errno + err(3). */
+	errno = e;
+	err(EXIT_FAILURE, "%scoding failed", decode ? "De": "En");
 }
 
 int
