--- a/usr.bin/nc/socks.c	2026-09-02 00:03:56.779255206 +0400
+++ b/usr.bin/nc/socks.c	2026-09-02 00:04:08.531448354 +0400
@@ -121,13 +121,12 @@
 
 	snprintf(prompt, sizeof(prompt), "Proxy password for %s@%s: ",
 	   proxyuser, proxyhost);
-#ifdef __NetBSD__
-     	if (getpassfd(prompt, pw, sizeof(pw), NULL, GETPASS_NEED_TTY, 0)
-	    == NULL)
-#else
-	if (readpassphrase(prompt, pw, sizeof(pw), RPP_REQUIRE_TTY) == NULL)
-#endif
-		err(1, "Unable to read proxy passphrase");
+	{
+		const char *p = getpass(prompt);
+		if (p == NULL)
+			err(1, "Unable to read proxy passphrase");
+		strlcpy(pw, p, sizeof(pw));
+	}
 	return (pw);
 }
 
@@ -185,7 +184,7 @@
 
 int
 socks_connect(const char *host, const char *port,
-    struct addrinfo hints __unused,
+    struct addrinfo hints,
     const char *proxyhost, const char *proxyport, struct addrinfo proxyhints,
     int socksv, const char *proxyuser)
 {
