--- a/usr.bin/find/ls.c
+++ b/usr.bin/find/ls.c
@@ -40,6 +40,7 @@
 
 #include <sys/param.h>
 #include <sys/stat.h>
+#include <sys/sysmacros.h>	/* FreeLinX: musl keeps major()/minor() here */
 
 #include <err.h>
 #include <errno.h>
@@ -50,8 +51,16 @@
 #include <stdlib.h>
 #include <string.h>
 #include <time.h>
-#include <tzfile.h>
 #include <unistd.h>
+
+/* FreeLinX: musl has no <tzfile.h>; NetBSD defines these time literals
+ * there.  SIXMONTHS below is built from them. */
+#ifndef SECSPERDAY
+#define	SECSPERDAY	(24 * 60 * 60)
+#endif
+#ifndef DAYSPERNYEAR
+#define	DAYSPERNYEAR	365
+#endif
 
 #include "find.h"
 
