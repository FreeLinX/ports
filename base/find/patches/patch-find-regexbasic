--- a/usr.bin/find/main.c
+++ b/usr.bin/find/main.c
@@ -56,6 +56,12 @@
 #include <string.h>
 #include <time.h>
 #include <unistd.h>
+
+/* FreeLinX: REG_BASIC is a BSD-only <regex.h> flag; musl's regcomp(3)
+ * compiles basic (BRE) expressions by default, and REG_BASIC == 0 there. */
+#ifndef REG_BASIC
+#define	REG_BASIC	0
+#endif
 
 #include "find.h"
 
