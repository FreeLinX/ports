--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1644,7 +1644,9 @@
     message(STATUS "Enabling custom link type: ${BINARY_LINK_TYPE}")
     target_compile_definitions(libfastfetch PUBLIC FF_DISABLE_DLOPEN=1)
     if(NOT WIN32)
-        set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -Wl,--copy-dt-needed-entries")
+        # FreeLinX: -Wl,--copy-dt-needed-entries is a GNU ld-only option that
+        # LLD does not implement (and it is the default behaviour anyway, so it
+        # is a no-op).  Omit it so the static musl link with clang + LLD passes.
     endif()
 endif()
 
