# FreeLinX/ports - base/cat : NetBSD `cat`.
#
# Metadata (see distinfo for the exact upstream source reference).
NAME   := cat
VERSION:= 10.1
CATEGORY:= base
LICENSE:= BSD-2-Clause
DEPENDENCIES:=
BUILD_DEPENDENCIES:=
TARGET := $(FREELINX_TRIPLE)
PREFIX := /

# Staging/work locations. The unpacked NetBSD src tree lands under SRC_DIR.
SRC_DIR  = $(FREELINX_BUILD_DIR)/work/$(NAME)
BUILD_BIN = $(SRC_DIR)/$(NAME)

include ../../mk/base-port.mk
