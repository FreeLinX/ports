# FreeLinX patches for base/awk (one-true-awk).
#
# Patches are applied after extraction into build/work/awk/.
# Name convention: patch-<description>
# Format: unified diff (-p1 from the SRC_DIR root).
