diff -ruN a/usr.bin/cksum/crc.c b/usr.bin/cksum/crc.c
--- a/usr.bin/cksum/crc.c
+++ b/usr.bin/cksum/crc.c
@@ -46,9 +46,19 @@
 #endif /* not lint */
 
 #include <sys/types.h>
-#include <sys/endian.h>
 
 #include <unistd.h>
+
+#include <stddef.h>
+#include <stdint.h>
+
+static uint32_t
+be32dec(const void *pp)
+{
+	const uint8_t *p = pp;
+	return ((uint32_t)p[0] << 24) | ((uint32_t)p[1] << 16) |
+	    ((uint32_t)p[2] << 8) | (uint32_t)p[3];
+}
 
 #include "extern.h"
 
