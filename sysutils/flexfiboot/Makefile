# FreeLinX/ports - sysutils/flexfiboot : FreeLinX Native UEFI Boot Manager
# Zero-dependency UEFI boot variable manager (efibootmgr replacement).

NAME    := flexfiboot
VERSION := 1.0
CATEGORY:= sysutils
LICENSE := MIT
TARGET  := $(FREELINX_TRIPLE)
PREFIX  := /usr

INSTALL_BIN:= flexfiboot
INSTALL_RELPATH:= usr/sbin/flexfiboot

include ../../mk/project-port.mk
