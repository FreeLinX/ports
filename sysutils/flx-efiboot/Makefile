# FreeLinX/ports - sysutils/flx-efiboot : FreeLinX Native UEFI Boot Manager
# Zero-dependency UEFI boot variable manager (efibootmgr replacement).

NAME    := flx-efiboot
VERSION := 1.0
CATEGORY:= sysutils
LICENSE := MIT
TARGET  := $(FREELINX_TRIPLE)
PREFIX  := /usr

INSTALL_BIN:= flx-efiboot
INSTALL_RELPATH:= usr/sbin/flx-efiboot

include ../../mk/project-port.mk
